# Local secrets — never commit real values
# Copy to .env and fill. Brevo key MUST stay backend-only.

# API / Postgres
DATABASE_URL=postgresql://user:pass@localhost:5432/email_templates

# Brevo — backend secret only (never VITE_/NEXT_PUBLIC_)
BREVO_API_KEY=

# Auth (ERP token verification — TBD)
# AUTH_JWKS_URL=
# AUTH_ISSUER=

# Asset storage (provider TBD)
# ASSET_STORAGE_PROVIDER=local
# ASSET_STORAGE_BUCKET=

# Ports (planned)
# EDITOR_PORT=5173
# API_PORT=3001
