# ECC Check — 2026-08-09 (security-hardening-audit)

## ECC Check — READY (with known residual gaps)

### Phase A (@test-gate)
- Depth: standard
- Result: **PASS** (`npm run checks` OK; new unit tests 9/9 pass)
- typedStrict: PASS on touched paths
- secureByDefault RG: PASS (no F-03/F-05/B-04/B-09 hits)
- Origin CSRF smoke: evil Origin → FORBIDDEN; no Origin → migrate OK

### Phase B (@verify-ticket)
- Acceptance: `.qa/acceptance/security-hardening-audit.md`
- Result: **PASS** for scoped hardening (sender allowlist, rate limit, origin, erp JWT, theme postMessage, migrate authz test)
- Residual (documented out of scope): HVAI Authorized_Works live mapping, production deploy, bulk Brevo republish

### Phase C (review)
- Verdict: **ACCEPT** for this ticket
- Medium B-07 sender allowlist: **fixed**
- Not ACCEPT for “full ERP drop-in”: JWT claim shape may need HVAI adapter tweak; no iframe shell in ERP yet

### Phase D (AgentShield)
- skipped — no `.cursor/` in this repo

### Phase E (@verify-ui)
- Smoke: API health + Origin gate verified live
- Full UI suite: not re-run (hardening mostly API); editor build PASS

### Phase E2 (memory-live-doc)
- skipped this run (note for `@ecc-check` apply later if committing)

### Ship
Ready for: **@commit-pr-safe** on a **feature branch** (not main) after user asks to commit.

**Still not ERP drop-in ready** until: HVAI JWT claim contract confirmed, iframe host wiring in HVAI-123 (read-only there), production cookies/CSRF if cookie sessions, deploy target.

### Implemented this round
- Server verified-sender allowlist (create/patch/publish/send-test)
- Rate limits on expensive POSTs
- Origin allowlist CSRF mitigation
- `AUTH_MODE=erp` HS256 Bearer JWT
- Theme `postMessage` listener `ets:theme`
- migrate-legacy-hashes permission unit test
