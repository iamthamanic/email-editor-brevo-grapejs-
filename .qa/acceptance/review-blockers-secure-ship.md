# Feature: Review blockers secure ship fixes

<!-- auto-generated by /implement on 2026-08-10 — do not edit manually unless correcting intent -->

## Intent
Behebe die Important/Critical Findings aus `@review-ticket` / `@review-security` / Bugbot, die ACCEPT und ECC READY blocken: F-02 RTE Autosync-Sanitize, B-01 JWT `exp` Pflicht, Brevo-Sync CONFLICT-Overwrite, Textbausteine-Delete-on-List, Asset-GET ohne Bearer für Canvas-`<img>`, Param-Host Autosave ohne stale JSON.

## Preconditions
- Uncommitted Working Tree mit RTE-Reentry-Fix vorhanden
- `npm run checks` lauffähig; API Unit-Tests für JWT/Sync vorhanden

## Happy Path
- [ ] `persistHostDomSilent` speichert nur `sanitizeEmailHtml(...)`-Ausgabe (kein raw `<script>` im Model)
- [ ] `verifyErpHs256Jwt` lehnt Tokens ohne numerisches `exp` ab; abgelaufenes `exp` weiterhin reject
- [ ] Zweiter Brevo-Sync bei `status=CONFLICT` überschreibt `editorData` nicht; aktualisiert nur `pendingRemote`
- [ ] `listSavedSections` löscht keine DB-Rows (Dedup nur in Response)
- [ ] `GET /api/assets/:uuid.ext` funktioniert ohne `Authorization` (Canvas-`<img>`); Upload bleibt authz-geschützt
- [ ] Autosave bei Text+Param-Host aktualisiert Model (guarded `syncContent` + Caret-Restore) statt still zu skippen

## Edge Cases
- [ ] Blur-Scrub erkennt auch `<script>` / `<iframe>` (nicht nur `on*` / `javascript:`)
- [ ] Asset-GET mit ungültigem Filename → 404; Path-Traversal weiterhin blocked
- [ ] JWT mit `exp` in der Zukunft → OK; ohne `exp` → throw

## Regression
- [ ] RTE Leave→Re-Enter Specs weiterhin grün
- [ ] `npm run checks` PASS
- [ ] Existing `erpJwt` / `syncConflict` unit tests PASS (+ neue Asserts)

## Assumptions
- Public Asset-GET ist OK wegen unguessable UUID-Filenames + SAFE_NAME + nosniff (Capability-URL); Upload bleibt `email_templates.edit`
- Param-Host Mid-Edit nutzt guarded `syncContent` (Caret-Restore) statt Silent-Flatten

## Security Coverage
- F-02: Sanitize in silent persist + erweiterter Blur-Scrub
- B-01: Pflicht-`exp` auf ERP JWT (fail-closed lifetime)
- B-02/B-03: Asset-Upload weiter permission-gated; GET capability-URL bewusst public
- P-04: Asset filename regex + path containment unverändert
- Out of scope: F-01 HTTPS, F-03/F-05 (keine neuen Secrets im Client), B-04 SQL, CSRF (unverändert)

## Screenshots
| Step | Filename |
|------|----------|
| 1 | n/a (API/security fixes; browser proof via existing RTE e2e) |

## Implementation Notes
- Files touched:
  - `packages/editor-core/src/rteSync.ts` — `sanitizeEmailHtml` in silent persist; structural hosts use guarded `syncContent`
  - `packages/email-components/src/register.ts` — blur scrub also catches `<script>`/`<iframe>`/`<object>`/`<embed>`
  - `apps/api/src/auth/erpJwt.ts` + test — require numeric `exp`
  - `apps/api/src/auth/dev-auth.ts` — public GET allowlist for UUID asset paths
  - `apps/api/src/assets/routes.ts` — GET no longer requires Bearer
  - `apps/api/src/templates/syncConflict.ts` + `brevoSync.ts` — `mustStashRemoteOnDiff` keeps CONFLICT drafts
  - `apps/api/src/saved-sections/service.ts` — list dedup without `deleteMany`
  - `docs/EMBED_CONTRACT.md` — document `exp` + public asset GET
- Unit tests: api 52 pass (new JWT exp + mustStashRemoteOnDiff); `npm run checks` PASS
- E2E: RTE re-entry / text-selection / preview-rte-sync green; bold toolbar occasionally flaky (pre-existing)
- Security: F-02, B-01 addressed; asset GET capability-URL documented
- Known limitations: structural mid-edit still briefly runs syncContent (caret restored by guard); style-only sanitize diffs do not rebuild on blur
